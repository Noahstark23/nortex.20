# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env
.env.*

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Artefactos de pruebas de mutación (Stryker)
.stryker-tmp/
reports/mutation/

# Evidencia local de QA visual y auditorías (no forma parte del artefacto)
.codex-audit/
.codex/audits/
artifacts/
design-qa.md

# El pipeline canónico usa npm + package-lock.json
pnpm-lock.yaml
pnpm-workspace.yaml

# Dumps locales del smoke de backup (CI y pruebas a mano)
tmp-backups/

# Evidencia local efímera de integración (fixtures y respuestas de QA).
reports/quality-*/
